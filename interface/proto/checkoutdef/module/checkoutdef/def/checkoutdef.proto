syntax = "proto3";

package checkoutdef.def;

enum PaymentGateway {
    UNSPECIFIED_GATEWAY = 0;
    PAG_SEGURO = 1;
}

enum PaymentMethods {
    UNSPECIFIED_METHOD = 0;
    PIX = 1;
}

enum PaymentStatus {
    UNSPECIFIED_STATUS = 0;
    WAITING = 1; 
    PAID = 2;
    REFUSED = 3;
    CANCELLED = 4;
    REFUNDED = 5;
}